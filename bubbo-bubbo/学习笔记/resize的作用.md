main.ts中resize方法的核心作用是：

1. **适配窗口尺寸变化**：根据当前窗口宽高与设计的最小尺寸（428×925），计算缩放比例，确保游戏画面在不同尺寸屏幕下均能完整显示且保持宽高比。

2. **保证内容清晰度**：当窗口尺寸小于最小设计尺寸时，通过将PIXI渲染器内部尺寸设为窗口尺寸×缩放比例（大于1），实现高分辨率渲染；同时保持canvas元素显示尺寸与窗口一致，借助浏览器自动压缩显示，从而在小屏幕上仍能呈现清晰画面。即超采样抗锯齿技术。

简言之，该方法通过动态调整渲染器尺寸和显示尺寸，平衡了跨设备适配的内容完整性与图像清晰度需求。
